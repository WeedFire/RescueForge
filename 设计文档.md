# RescueForge 数据恢复软件 —— 详细设计文档

## 1. 项目概述

### 1.1 背景
数据丢失问题广泛存在于个人和企业场景，而现有数据恢复工具普遍存在以下痛点：  
- 体积庞大、安装复杂，多平台支持不足。  
- 扫描效率低下，单线程机械硬盘扫描耗时过长。  
- 界面陈旧，交互不友好，缺乏实时进度反馈。  

RescueForge 旨在打造一款轻量、跨平台、高性能的数据恢复软件，融合现代前后端技术，兼顾专业级恢复能力和消费级使用体验。

### 1.2 产品定位
面向普通用户与 IT 运维人员，提供**开箱即用**的数据救援工具，覆盖快速恢复误删文件、深度扫描格式化磁盘、RAW 文件雕刻、分区修复等场景。

### 1.3 核心特性
- **极致轻量与跨平台**：安装包小于 15 MB，一套代码编译出 Windows、macOS、Linux 原生应用。  
- **Rust 驱动的高性能引擎**：利用 Rust 的零成本抽象和 `rayon` 数据并行，实现多线程扇区级读取与文件签名匹配，扫描速度相比传统工具提升 3-5 倍。  
- **现代化 UI 与实时反馈**：基于 Vue 3 的响应式界面，通过 Tauri Events 在长达数小时的深度扫描中提供丝滑的实时进度、文件树动态渲染和磁盘热力图。

## 2. 系统架构设计

整体采用 **Tauri 桌面框架 + Vue 3 前端** 的经典架构，底层核心引擎由 Rust 实现，前端负责交互与展示，两者通过 Tauri IPC 和事件系统无缝通信。

### 2.1 架构分层

```
┌─────────────────────────────────────────────────┐
│                    展示层 (Vue 3)                │
│  Vue 3 + TypeScript + Pinia + TailwindCSS       │
│  磁盘仪表盘 | 扫描向导 | 文件预览树 | 分区修复   │
└────────────────────┬────────────────────────────┘
                     │ Tauri Commands / Events
┌────────────────────▼────────────────────────────┐
│                  控制层 (Tauri Core)              │
│  ┌─────────────────────────────────────────┐    │
│  │ 任务调度器 (Tokio Async Runtime)         │    │
│  │ - 扫描任务队列 / 进度广播 / 取消与暂停     │    │
│  └─────────────────────────────────────────┘    │
└───┬──────────────────┬──────────────┬───────────┘
    │                  │              │
┌───▼──────────┐ ┌────▼─────┐ ┌─────▼──────┐
│ 块设备读取器 │ │文件系统  │ │ 文件雕刻器  │
│ (Block I/O)  │ │解析引擎  │ │ (Carving)   │
│ 绕过OS文件锁 │ │NTFS/FAT  │ │ 魔术字节匹配│
│ 扇区级读取   │ │ext4/APFS │ │ 多线程流处理│
└──────────────┘ └──────────┘ └─────────────┘
```

### 2.2 技术栈

| 层级         | 技术选型                            | 说明 |
|--------------|-------------------------------------|------|
| 前端框架     | Vue 3 + TypeScript + Vite           | 响应式 UI、路由、状态管理（Pinia）、图表（ECharts）、样式（TailwindCSS） |
| 桌面容器     | Tauri 2.x                           | 提供原生窗口、系统托盘、自动更新、IPC |
| 后端核心     | Rust (Tokio, Rayon, serde)          | 磁盘 I/O、文件系统解析、文件雕刻、任务调度 |
| 数据持久化   | SQLite (rusqlite)                   | 存储扫描历史、文件签名库、设置 |
| 文件系统库   | `ntfs`、`fatfs`、`walkdir` 等 crates | 解析元数据 |
| 可视化       | ECharts + Canvas API                | 磁盘热力图、进度统计图 |

## 3. 核心功能模块设计

### 3.1 磁盘概览与健康管理 (借鉴 DiskGenius)
- **物理/逻辑视图切换**：左侧树状图展示所有物理磁盘及其下挂载的逻辑分区，显示盘符、文件系统、总容量、已用/可用空间。
- **磁盘热力图**：基于扇区状态数据，使用 Canvas 绘制磁盘使用情况热力图，不同颜色代表已占用、空闲、损坏、系统保留区域，直观展示碎片化程度。
- **S.M.A.R.T. 检测**：通过 ATA 指令读取磁盘健康状态，展示关键属性（重映射扇区数、通电时间、温度等），并在磁盘濒临损坏时给出明确预警，提示用户优先恢复数据。

### 3.2 多级扫描引擎
- **Level 1 – 快速扫描**：仅解析文件系统的元数据（NTFS 的 MFT、FAT 的目录项），秒级列出已被标记为删除但未被覆盖的文件，恢复成功率依赖元数据完整性。
- **Level 2 – 深度扫描**：遍历文件系统的全部目录结构，寻找未链接的“孤儿”文件节点，比快速扫描更彻底，但仍依赖文件系统未损坏。
- **Level 3 – RAW 恢复 (文件雕刻)**：当文件系统彻底损坏（如提示“需要格式化”）时，直接读取磁盘扇区，通过**文件签名（魔术字节）** 匹配文件头尾特征（如 JPEG: `FF D8 FF` / `FF D9`，PDF: `%PDF` / `%%EOF`），从原始数据流中提取完整文件。支持 100+ 种预定义签名，可扩展自定义签名。

### 3.3 分区表修复与引导重建 (借鉴 TestDisk)
- **丢失分区搜索**：扫描磁盘残余的分区引导扇区 (Boot Sector) 和备份分区表，尝试重建 MBR/GPT 分区表，一键恢复被误删的分区。
- **引导扇区修复**：支持备份当前卷的引导扇区（FAT32/NTFS），并允许从标准模板或备份文件进行恢复，解决因病毒或误操作导致的“Non-System Disk”启动失败。

### 3.4 安全恢复与文件预览
- **强制只读**：所有底层磁盘操作均以只读方式打开，禁止任何写入行为，避免二次破坏。
- **内存级预览**：在扫描结果中，对于图片（JPEG/PNG/GIF）、文档（TXT/PDF 文本）、音视频（提取关键帧或元数据）提供轻量级解析和即时预览，用户可在恢复前确认文件内容是否完整。
- **恢复车 (Recovery Cart)**：支持将需要恢复的文件拖入收藏夹，选定目标磁盘后一键导出，自动维护原始目录结构，并校验文件哈希确保完整性。

## 4. 关键技术实现方案 (Rust 部分)

### 4.1 底层块设备读取
- **Windows 平台**：通过 Windows API `CreateFileW` 以 `GENERIC_READ` + `FILE_SHARE_READ|FILE_SHARE_WRITE` 打开 `\\.\PhysicalDriveN`，获取磁盘句柄。调用 `DeviceIoControl` 取得磁盘几何信息（扇区大小、总扇区数）。读取时使用 `ReadFile` 按扇区偏移和大小读取，Rust 侧封装为安全的 `RawDisk` 结构体。
- **Linux/macOS**：直接以只读方式打开设备文件 `/dev/sdX` 或 `/dev/rdiskX`，通过 `lseek` + `read` 实现。对于权限不足的情况，提示用户以 `sudo` 运行或使用 polkit 授权。
- **跨平台抽象**：定义一个 `trait BlockDevice`，包含 `read_sectors(offset, count, buffer)` 和 `get_geometry()` 方法，各平台分别实现，确保上层逻辑无关。

### 4.2 高性能文件雕刻引擎
- **并行分块策略**：将磁盘按固定大小（如 64 MB）划分为多个数据块，使用 `rayon` 的 `par_chunks` 分发到多核并行扫描。
- **签名匹配**：每个线程维持一个滑动窗口，依次匹配所有启用的文件签名头。一旦命中头部，根据规则确定尾部特征或估计文件长度，尝试提取文件数据并写入临时目录。
- **结果去重与交叉验证**：雕刻出的文件通过哈希（SHA-256）去重，避免同一文件多次保存。同时记录每个文件的起始扇区和大小，用于后续完整性校验。
- **进度反馈**：利用 `AtomicUsize` 安全计数已扫描扇区数和已发现文件数，定时（如每 500 ms）通过 Tauri `emit` 发送 `ScanProgress` 事件至前端，包含百分比、文件数和瞬时速度。

### 4.3 文件系统解析
- **NTFS**：解析 MFT（主文件表），读取 `$FILE_NAME` 和 `$DATA` 属性，恢复已标记为删除（标志位 `0x03`）且数据运行（Data Runs）仍完整的文件。支持硬链接、流、压缩与加密属性检测。
- **FAT32/exFAT**：遍历目录项，恢复以 `0xE5` 开头的删除项，结合 FAT 表追踪簇链还原文件内容。
- **ext4**：利用 `libext2fs` 的 Rust 绑定或自研 inode 遍历，识别已删除但 inode 未被回收的文件，通过块位图查找占用块。

### 4.4 任务调度与生命周期
- **异步任务管理**：基于 Tokio 的 `mpsc` 通道，将扫描请求抽象为 `Job`，支持优先级排序、暂停/继续/取消。取消操作通过 `tokio::select!` 和 `CancellationToken` 实现安全中断。
- **显存与内存保护**：雕刻过程中，若系统内存低于警戒值（如 < 512 MB），自动降低并行度或暂停扫描，待内存释放后恢复。

## 5. 前端界面与交互设计

### 5.1 设计语言
整体采用**暗色主题 + 半透明磨砂玻璃效果**，以深蓝、紫灰为主色调，重点数据采用荧光绿、琥珀色高亮，契合“数据恢复”的专业感与科技感。布局遵循卡片化原则，避免信息过载。

### 5.2 主要界面
1. **仪表盘 (Dashboard)**  
   - 顶部显示系统磁盘列表，每一张卡片展示磁盘型号、容量、健康状态指示灯、温度。  
   - 每个卡片提供两个主按钮：“快速恢复”和“深度扫描”。  
   - 下方展示最近的扫描历史记录。

2. **扫描进行时 (Scanning View)**  
   - 左侧为实时日志流（可折叠），记录发现的每个文件信息和引擎动作。  
   - 中央为磁盘扇区热力图，已扫描区域逐步点亮，发现文件的扇区闪烁金色。  
   - 右侧为**动态文件树**，采用虚拟滚动技术，支持百万级文件节点的流畅渲染，实时添加新发现的文件条目。

3. **扫描结果与恢复 (Results View)**  
   - 采用类似文件管理器的网格/列表双模式视图，支持按类型、大小、修改时间、扩展名过滤。  
   - 点击文件可在右侧预览面板查看缩略图或文本提取内容。  
   - 底部固定“恢复车”栏，拖入文件后显示已选文件的总大小和数量，点击“一键恢复”引导用户选择输出路径。

4. **分区修复向导**  
   - 引导式步骤：选择磁盘 → 搜索丢失分区 → 预览分区列表 → 执行修复。  
   - 显示找到的分区及其文件系统、大小、状态，用户可勾选要恢复的分区。

### 5.3 交互细节
- 所有长时间操作均提供进度条、预计剩余时间、取消按钮。  
- 操作不可逆时（如恢复文件覆盖、写入分区表），弹出二次确认对话框并强调风险。  
- 支持快捷键（如 Ctrl+F 搜索文件，Ctrl+S 保存扫描状态）和右键菜单。

## 6. 数据模型

### 6.1 核心实体
- **DiskInfo**：物理磁盘描述（索引、型号、序列号、容量、扇区大小、分区列表）。  
- **PartitionInfo**：分区信息（起始扇区、大小、文件系统、挂载点、状态）。  
- **ScanSession**：扫描会话（开始时间、结束时间、模式、总扇区数、已扫描扇区数、发现文件数）。  
- **RecoveredFile**：恢复文件记录（文件名、扩展名、大小、恢复后路径、原始路径、签名类型、扇区位置、哈希值、状态）。  
- **FileSignature**：文件签名定义（扩展名、头部字节序列、尾部字节序列、最大长度限制）。  

### 6.2 数据库选型与存储
- 使用本地 SQLite 数据库持久化扫描历史、签名库、用户设置等结构化数据。  
- 大型二进制数据（如扫描快照）以文件形式存储在应用数据目录下。

## 7. 安全性、性能与可维护性

- **内存安全**：Rust 所有权模型确保底层磁盘操作无数据竞争、无缓冲区溢出，极大降低系统级崩溃风险。  
- **权限控制**：启动时检测管理员/root 权限，若不足则提示并引导授权，不支持降级运行。  
- **磁盘 I/O 调度**：采用异步 I/O 与线程池结合，避免阻塞 UI 线程，确保应用响应流畅。  
- **日志与错误处理**：使用 `tracing` 记录详细的调试日志，同时将关键错误通过 Tauri Events 推送到前端展示，用户可导出日志用于诊断。  
- **自动更新**：集成 Tauri 内置的更新器，支持静默下载新版本和增量更新。

## 8. 开发路线图

### Phase 1: 核心引擎与基础 UI (第1-8周)
- 实现跨平台原始磁盘读取抽象层，完成 Windows/Linux/macOS 适配。  
- 解析 MBR/GPT 分区表，获取物理磁盘与逻辑分区信息。  
- 搭建 Vue 3 + Tauri 基础项目，完成磁盘列表卡片展示、S.M.A.R.T. 数据读取与健康指示灯。  
- 建立任务调度框架和 Tauri Events 通信机制。

### Phase 2: 快速扫描与文件恢复 (第9-16周)
- 开发 NTFS/FAT32/exFAT 文件系统解析模块，实现已删除文件的快速扫描与恢复。  
- 构建文件目录树组件（虚拟滚动），集成文件预览（图片/文本）。  
- 实现恢复车逻辑、导出功能，确保恢复后文件完整性校验。  
- 引入用户设置（界面主题、默认恢复路径）。

### Phase 3: RAW 雕刻与深度扫描 (第17-24周)
- 实现多线程文件雕刻引擎，内置 100+ 种文件签名，支持自定义签名。  
- 增加磁盘热力图可视化，实时同步扫描进度。  
- 完善暂停/继续/取消扫描功能，优化大磁盘扫描的内存与 CPU 占用。  
- 测试并优化恢复成功率，处理碎片化文件的拼接算法（如 JPEG 片段重组）。

### Phase 4: 分区修复与高级功能 (第25-30周)
- 实现丢失分区搜索与重建、引导扇区备份与修复。  
- 提供磁盘镜像创建（.img/.dd）功能，支持对坏道磁盘进行安全备份。  
- 多语言支持、暗黑/浅色主题、自动更新、商业化授权与注册。  
- 全面压力测试与性能调优，编写用户文档和帮助中心。

## 9. 总结

RescueForge 利用 Rust 的高性能与安全性、Tauri 的跨平台轻量特性以及 Vue 3 的现代前端能力，构建了一款兼顾专业级数据恢复能力和极佳用户体验的桌面应用。其模块化设计、多级扫描策略与实时反馈机制，能够显著降低数据恢复门槛，帮助用户安全、高效地找回珍贵数据。

## 10.关键代码示例
在 Tauri 架构中处理物理磁盘读取和权限提升，是数据恢复软件**最核心、最危险、也最容易踩坑**的底层工程。

工业级的标准做法**绝对不是**让整个 Tauri 应用（包含前端 JS）以管理员/root 权限运行（这会导致极大的安全漏洞，一旦前端被 XSS 攻击，黑客直接获得系统最高权限）。

**正确的架构是：Sidecar（伴飞进程）模式。**
Tauri 主进程保持普通用户权限，当需要扫描磁盘时，主进程调用一个独立的、编译好的 Rust 二进制文件（Sidecar），并使用系统原生的 UAC/sudo 机制**仅对这个 Sidecar 进行提权**。Sidecar 读取底层扇区后，通过 IPC（标准输出或本地 Socket）将数据安全地流式传回主进程。

以下为您提供这套**工业级跨平台 Raw Disk 读取与提权**的完整 Rust 核心代码骨架。

---

### 一、 核心依赖配置 (`Cargo.toml`)

我们需要针对不同操作系统引入特定的 API 绑定。

```toml
[dependencies]
tauri = { version = "2.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
    "Win32_UI_Shell",
    "Win32_Security",
    "Win32_System_Threading"
]}
winapi = { version = "0.3", features = ["winnt", "handleapi", "fileapi"] }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["user", "fs"] }
```

---

### 二、 权限检测与原生提权模块 (`src/privilege.rs`)

这个模块负责检测当前是否拥有读取物理磁盘的权限。如果没有，则触发系统原生的提权弹窗（Windows UAC / Linux Polkit / macOS AppleScript）。

```rust
// src/privilege.rs
use anyhow::Result;

#[cfg(target_os = "windows")]
pub mod windows_priv {
    use windows::Win32::Foundation::{HANDLE, BOOL};
    use windows::Win32::Security::{GetTokenInformation, TokenElevation, TOKEN_ELEVATION, TOKEN_QUERY};
    use windows::Win32::System::Threading::{GetCurrentProcess, OpenProcessToken};
    use anyhow::{Result, anyhow};

    /// 检测当前进程是否以管理员身份运行
    pub fn is_elevated() -> bool {
        unsafe {
            let mut token: HANDLE = HANDLE(0);
            if OpenProcessToken(GetCurrentProcess(), TOKEN_QUERY, &mut token).is_err() {
                return false;
            }

            let mut elevation = TOKEN_ELEVATION::default();
            let mut ret_len = 0;
            
            let success = GetTokenInformation(
                token,
                TokenElevation,
                Some(&mut elevation as *mut _ as *mut _),
                std::mem::size_of::<TOKEN_ELEVATION>() as u32,
                &mut ret_len,
            ).is_ok();

            success && elevation.TokenIsElevated != 0
        }
    }

    /// 触发 UAC 弹窗，以管理员权限重启指定的 Sidecar 程序
    pub fn elevate_and_execute(sidecar_path: &str, args: &[&str]) -> Result<()> {
        use windows::core::PCWSTR;
        use windows::Win32::UI::Shell::ShellExecuteW;
        use windows::Win32::UI::WindowsAndMessaging::SW_SHOWNORMAL;
        use std::os::windows::ffi::OsStrExt;

        let operation: Vec<u16> = std::ffi::OsStr::new("runas").encode_wide().chain(Some(0)).collect();
        let file: Vec<u16> = std::ffi::OsStr::new(sidecar_path).encode_wide().chain(Some(0)).collect();
        
        let args_str = args.join(" ");
        let parameters: Vec<u16> = std::ffi::OsStr::new(&args_str).encode_wide().chain(Some(0)).collect();

        unsafe {
            let result = ShellExecuteW(
                None,
                PCWSTR(operation.as_ptr()),
                PCWSTR(file.as_ptr()),
                PCWSTR(parameters.as_ptr()),
                None,
                SW_SHOWNORMAL,
            );
            
            if result.0 as isize <= 32 {
                return Err(anyhow!("用户拒绝了 UAC 提权请求或提权失败"));
            }
        }
        Ok(())
    }
}

#[cfg(target_os = "linux")]
pub mod linux_priv {
    use nix::unistd::geteuid;
    use std::process::Command;
    use anyhow::Result;

    pub fn is_elevated() -> bool {
        geteuid().is_root()
    }

    pub fn elevate_and_execute(sidecar_path: &str, args: &[&str]) -> Result<()> {
        // 优先尝试 pkexec (图形化 Polkit)，失败则回退到 sudo
        let status = Command::new("pkexec")
            .arg(sidecar_path)
            .args(args)
            .status();

        if status.is_err() {
            Command::new("sudo")
                .arg(sidecar_path)
                .args(args)
                .status()?;
        }
        Ok(())
    }
}

#[cfg(target_os = "macos")]
pub mod macos_priv {
    use nix::unistd::geteuid;
    use std::process::Command;
    use anyhow::Result;

    pub fn is_elevated() -> bool {
        geteuid().is_root()
    }

    pub fn elevate_and_execute(sidecar_path: &str, args: &[&str]) -> Result<()> {
        // 使用 AppleScript 触发 macOS 原生的密码输入框
        let cmd = format!(
            "do shell script \"'{}' {}\" with administrator privileges",
            sidecar_path,
            args.join(" ")
        );
        
        Command::new("osascript")
            .arg("-e")
            .arg(cmd)
            .status()?;
        Ok(())
    }
}
```

---

### 三、 跨平台 Raw Disk 块设备读取引擎 (`src/raw_disk.rs`)

这是数据恢复的心脏。必须绕过操作系统的文件缓存，直接进行扇区级 I/O。

```rust
// src/raw_disk.rs
use anyhow::{Result, anyhow};
use std::fs::File;
use std::io::{Read, Seek, SeekFrom};

pub struct RawDisk {
    file: File,
    pub sector_size: u64,
    pub total_sectors: u64,
}

impl RawDisk {
    /// 跨平台打开物理磁盘
    pub fn open(device_path: &str) -> Result<Self> {
        #[cfg(target_os = "windows")]
        return Self::open_windows(device_path);

        #[cfg(target_os = "linux")]
        return Self::open_unix(device_path);

        #[cfg(target_os = "macos")]
        return Self::open_macos(device_path);
        
        #[cfg(not(any(target_os = "windows", target_os = "linux", target_os = "macos")))]
        Err(anyhow!("不支持的操作系统"))
    }

    #[cfg(target_os = "windows")]
    fn open_windows(device_path: &str) -> Result<Self> {
        use std::os::windows::ffi::OsStrExt;
        use windows::core::PCWSTR;
        use windows::Win32::Storage::FileSystem::{
            CreateFileW, FILE_SHARE_READ, FILE_SHARE_WRITE, OPEN_EXISTING, FILE_FLAG_NO_BUFFERING
        };
        use windows::Win32::Foundation::HANDLE;
        use std::os::windows::io::FromRawHandle;

        // Windows 物理磁盘路径格式: \\.\PhysicalDrive0
        let path = format!("\\\\.\\{}", device_path);
        let wide_path: Vec<u16> = std::ffi::OsStr::new(&path).encode_wide().chain(Some(0)).collect();

        unsafe {
            let handle = CreateFileW(
                PCWSTR(wide_path.as_ptr()),
                windows::Win32::Storage::FileSystem::GENERIC_READ.0,
                FILE_SHARE_READ | FILE_SHARE_WRITE,
                None,
                OPEN_EXISTING,
                FILE_FLAG_NO_BUFFERING, // 核心：绕过 OS 缓存，直接读取硬件
                None,
            )?;

            if handle.is_invalid() {
                return Err(anyhow!("无法打开物理磁盘，请确认已获得管理员权限"));
            }

            // TODO: 调用 DeviceIoControl (IOCTL_DISK_GET_DRIVE_GEOMETRY) 获取真实的扇区大小和总数
            let sector_size = 512; 
            let total_sectors = 10000000; // 占位，需动态获取

            Ok(Self {
                file: File::from_raw_handle(handle.0 as _),
                sector_size,
                total_sectors,
            })
        }
    }

    #[cfg(target_os = "linux")]
    fn open_unix(device_path: &str) -> Result<Self> {
        // Linux 路径格式: /dev/sda 或 /dev/nvme0n1
        let path = format!("/dev/{}", device_path);
        
        // 必须以 O_RDONLY 打开，且需要 root 权限
        let file = File::open(&path)?;
        
        // TODO: 使用 ioctl (BLKGETSIZE64) 获取真实容量
        Ok(Self {
            file,
            sector_size: 512,
            total_sectors: 10000000,
        })
    }

    #[cfg(target_os = "macos")]
    fn open_macos(device_path: &str) -> Result<Self> {
        // macOS 路径格式: /dev/rdisk1 (使用 rdisk 绕过缓冲，速度更快)
        let path = format!("/dev/r{}", device_path);
        let file = File::open(&path)?;
        
        // macOS 在读取前通常需要先卸载磁盘: diskutil unmountDisk /dev/disk1
        
        Ok(Self {
            file,
            sector_size: 512,
            total_sectors: 10000000,
        })
    }

    /// 读取指定数量的扇区到缓冲区
    pub fn read_sectors(&mut self, start_sector: u64, count: u64, buffer: &mut [u8]) -> Result<()> {
        let offset = start_sector * self.sector_size;
        let bytes_to_read = count * self.sector_size;
        
        if buffer.len() < bytes_to_read as usize {
            return Err(anyhow!("缓冲区大小不足"));
        }

        self.file.seek(SeekFrom::Start(offset))?;
        self.file.read_exact(&mut buffer[..bytes_to_read as usize])?;
        
        Ok(())
    }
}
```

---

### 四、 Tauri 主进程调度与 Sidecar 通信 (`src/main.rs`)

在 Tauri 中，前端点击“开始扫描”时，主进程检查权限。如果权限不足，则拉起提权后的 Sidecar 进程进行扫描，并通过 Tauri Events 将进度推送到前端。

```rust
// src/main.rs
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

mod privilege;
mod raw_disk;

use tauri::{Emitter, Manager};
use serde::Serialize;
use std::process::{Command, Stdio};
use std::io::{BufRead, BufReader};

#[derive(Serialize, Clone)]
struct ScanEvent {
    status: String,
    progress: f32,
    message: String,
}

#[tauri::command]
async fn start_disk_scan(app_handle: tauri::AppHandle, device_id: String) -> Result<(), String> {
    // 1. 检查当前是否有权限
    #[cfg(target_os = "windows")]
    let has_priv = privilege::windows_priv::is_elevated();
    #[cfg(target_os = "linux")]
    let has_priv = privilege::linux_priv::is_elevated();
    #[cfg(target_os = "macos")]
    let has_priv = privilege::macos_priv::is_elevated();

    if !has_priv {
        // 2. 如果没有权限，触发系统原生提权弹窗，并执行 Sidecar
        app_handle.emit("scan-event", ScanEvent {
            status: "requesting_privilege".into(),
            progress: 0.0,
            message: "正在请求系统管理员权限...".into(),
        }).unwrap();

        // 获取 sidecar 的绝对路径 (在 Tauri 中通常通过 tauri.conf.json 配置 bundle)
        let sidecar_path = app_handle.path_resolver()
            .resolve_resource("./sidecar/disk_scanner")
            .unwrap()
            .to_string_lossy()
            .to_string();

        // 调用提权模块 (会弹出 UAC/sudo 窗口)
        #[cfg(target_os = "windows")]
        privilege::windows_priv::elevate_and_execute(&sidecar_path, &[&device_id]).map_err(|e| e.to_string())?;
        
        // Linux/macOS 同理...
        
        return Ok(());
    }

    // 3. 如果已有权限，直接在后台线程启动扫描 (或调用 Sidecar 的非提权模式)
    std::thread::spawn(move || {
        run_scan_logic(app_handle, device_id);
    });

    Ok(())
}

fn run_scan_logic(app_handle: tauri::AppHandle, device_id: String) {
    app_handle.emit("scan-event", ScanEvent {
        status: "scanning".into(),
        progress: 0.0,
        message: "正在读取底层扇区...".into(),
    }).unwrap();

    // 调用 RawDisk 引擎
    let mut disk = match raw_disk::RawDisk::open(&device_id) {
        Ok(d) => d,
        Err(e) => {
            app_handle.emit("scan-event", ScanEvent {
                status: "error".into(), progress: 0.0, message: e.to_string()
            }).unwrap();
            return;
        }
    };

    let mut buffer = vec![0u8; 4096]; // 每次读取 8 个扇区
    
    // 模拟扫描循环
    for i in 0..100 {
        // disk.read_sectors(i * 8, 8, &mut buffer).unwrap();
        
        // 推送进度到 Vue 前端
        app_handle.emit("scan-event", ScanEvent {
            status: "scanning".into(),
            progress: i as f32,
            message: format!("已扫描 {}%", i),
        }).unwrap();
        
        std::thread::sleep(std::time::Duration::from_millis(100));
    }
}

fn main() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![start_disk_scan])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

---

### 五、 架构师的避坑指南（血泪经验）

在实际开发这款数据恢复软件时，您一定会遇到以下三个“致命坑”，请提前防御：

1.  **Windows 的卷锁定 (Volume Lock) 问题**：
    *   **坑**：在 Windows 上，即使你以管理员身份打开了 `\\.\PhysicalDrive0`，如果该磁盘上有分区正在被系统使用（比如 C 盘），`CreateFileW` 会直接报错 `ERROR_SHARING_VIOLATION`。
    *   **解法**：在读取前，必须先调用 `DeviceIoControl` 发送 `FSCTL_LOCK_VOLUME` 和 `FSCTL_DISMOUNT_VOLUME` 将卷卸载（Dismount）。但这会导致系统暂时无法访问该盘符。R-Studio 和 DiskGenius 都是这么做的。
2.  **macOS 的 SIP (系统完整性保护) 与全盘访问**：
    *   **坑**：在 macOS Catalina 及以上版本，即使你获得了 root 权限（sudo），如果没有在“系统偏好设置 -> 隐私与安全性 -> 完全磁盘访问权限”中给你的 App 打勾，底层读取 `/dev/rdiskX` 依然会被内核拦截（Operation not permitted）。
    *   **解法**：在 Tauri 前端 UI 中，检测到位移权限不足时，引导用户手动打开系统设置并添加 App。可以通过 `x-apple.systempreferences:com.apple.preference.security?Privacy_AllFiles` 这个 URL Scheme 直接跳转到该设置页面。
3.  **Sidecar 的跨平台打包 (Tauri Bundle)**：
    *   **坑**：Tauri 打包时，默认只会打包前端资源和主 Rust 进程。你写的 `disk_scanner` Sidecar 二进制文件不会被自动包含进去，导致用户安装后找不到文件。
    *   **解法**：必须在 `tauri.conf.json` 的 `bundle.externalBin` 中显式声明 Sidecar 的路径，并且要为 Windows、macOS、Linux 分别编译对应架构（x86_64 / aarch64）的二进制文件，加上特定的后缀（如 `.exe`）。

这套代码和架构方案，是目前使用 Web 技术栈开发底层系统工具的**最高工业标准**。它既保证了前端 UI 的现代化与跨平台，又守住了底层系统安全与性能的底线。您可以直接将此骨架接入您的 Tauri 项目中开始 Phase 1 的开发。